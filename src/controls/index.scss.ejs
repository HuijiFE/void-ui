/**
 * All controls style for Void-UI.
 */

<%-
globby
  .sync(['src/controls/*/**/*.scss'])
  .filter(file => !/\/base\//.test(file))
  .map(file => `@import '~${toAlias(file.replace(/\.scss$/, ''))}';`)
  .join('\n');
%>
